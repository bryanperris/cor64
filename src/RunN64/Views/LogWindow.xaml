<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="RunN64.Views.LogWindow"
        Title="System Log"
        Width="700"
        Height="300">
    <ListBox Name="logView" Background="Black" Items="{Binding Items}" AutoScrollToSelectedItem="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <TextBlock FontFamily="Monospace" FontSize="12" Foreground="{Binding Color}" Text="{Binding Message}" TextWrapping="Wrap"/>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</Window>
